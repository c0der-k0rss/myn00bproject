// Script to shutdown mac - will need to create a bash script //

#!/bin/bash

echo -n "Enter a number > "
read x
echo [your password] | sudo -S shutdown -h +$x

// Shutdown Ubuntu machine script //

os.system('shutdown -h now')

 import os

 if condition:
   os.system("shutdown -h now")

//
